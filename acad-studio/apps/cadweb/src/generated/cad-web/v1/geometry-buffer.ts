// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { BlockDefinition, BlockDefinitionT } from '../../cad-web/v1/block-definition';
import { BufferKind } from '../../cad-web/v1/buffer-kind';
import { Entity, EntityT } from '../../cad-web/v1/entity';


export class GeometryBuffer implements flatbuffers.IUnpackableObject<GeometryBufferT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GeometryBuffer {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGeometryBuffer(bb:flatbuffers.ByteBuffer, obj?:GeometryBuffer):GeometryBuffer {
  return (obj || new GeometryBuffer()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGeometryBuffer(bb:flatbuffers.ByteBuffer, obj?:GeometryBuffer):GeometryBuffer {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GeometryBuffer()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('CWEB');
}

schemaVersion():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 1;
}

kind():BufferKind {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : BufferKind.Unknown;
}

entities(index: number, obj?:Entity):Entity|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Entity()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

entitiesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

blocks(index: number, obj?:BlockDefinition):BlockDefinition|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new BlockDefinition()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

blocksLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startGeometryBuffer(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addSchemaVersion(builder:flatbuffers.Builder, schemaVersion:number) {
  builder.addFieldInt32(0, schemaVersion, 1);
}

static addKind(builder:flatbuffers.Builder, kind:BufferKind) {
  builder.addFieldInt8(1, kind, BufferKind.Unknown);
}

static addEntities(builder:flatbuffers.Builder, entitiesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, entitiesOffset, 0);
}

static createEntitiesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEntitiesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addBlocks(builder:flatbuffers.Builder, blocksOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, blocksOffset, 0);
}

static createBlocksVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startBlocksVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endGeometryBuffer(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishGeometryBufferBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'CWEB');
}

static finishSizePrefixedGeometryBufferBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'CWEB', true);
}

static createGeometryBuffer(builder:flatbuffers.Builder, schemaVersion:number, kind:BufferKind, entitiesOffset:flatbuffers.Offset, blocksOffset:flatbuffers.Offset):flatbuffers.Offset {
  GeometryBuffer.startGeometryBuffer(builder);
  GeometryBuffer.addSchemaVersion(builder, schemaVersion);
  GeometryBuffer.addKind(builder, kind);
  GeometryBuffer.addEntities(builder, entitiesOffset);
  GeometryBuffer.addBlocks(builder, blocksOffset);
  return GeometryBuffer.endGeometryBuffer(builder);
}

unpack(): GeometryBufferT {
  return new GeometryBufferT(
    this.schemaVersion(),
    this.kind(),
    this.bb!.createObjList<Entity, EntityT>(this.entities.bind(this), this.entitiesLength()),
    this.bb!.createObjList<BlockDefinition, BlockDefinitionT>(this.blocks.bind(this), this.blocksLength())
  );
}


unpackTo(_o: GeometryBufferT): void {
  _o.schemaVersion = this.schemaVersion();
  _o.kind = this.kind();
  _o.entities = this.bb!.createObjList<Entity, EntityT>(this.entities.bind(this), this.entitiesLength());
  _o.blocks = this.bb!.createObjList<BlockDefinition, BlockDefinitionT>(this.blocks.bind(this), this.blocksLength());
}
}

export class GeometryBufferT implements flatbuffers.IGeneratedObject {
constructor(
  public schemaVersion: number = 1,
  public kind: BufferKind = BufferKind.Unknown,
  public entities: (EntityT)[] = [],
  public blocks: (BlockDefinitionT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const entities = GeometryBuffer.createEntitiesVector(builder, builder.createObjectOffsetList(this.entities));
  const blocks = GeometryBuffer.createBlocksVector(builder, builder.createObjectOffsetList(this.blocks));

  return GeometryBuffer.createGeometryBuffer(builder,
    this.schemaVersion,
    this.kind,
    entities,
    blocks
  );
}
}
