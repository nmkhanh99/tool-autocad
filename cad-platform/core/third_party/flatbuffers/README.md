# FlatBuffers runtime

The `include/flatbuffers` directory and `LICENSE` are copied unchanged from
Google FlatBuffers release `v25.9.23`. Only the header-only C++ runtime is
vendored; the compiler is not part of this repository.

Source: <https://github.com/google/flatbuffers/releases/tag/v25.9.23>
