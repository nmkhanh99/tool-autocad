  3 thứ cần lấy từ công ty (không suy ra được từ file)

  1. Bảng layer chuẩn chính thức (để chốt F4).
  2. Template Excel BOM đang dùng (để khớp đầu ra F3).
  3. Bộ block chuẩn MEP (bắt buộc nếu muốn F2).

   HTOOLS chạy được vì nó là plugin .NET — mà .NET/PaletteSet/WPF

    AutoCAD (Tool Palette)